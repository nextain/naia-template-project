# progress — 작업 기록 (ledger)

날짜별 진행·검토·설계 메모를 여기 둔다 (`<주제>-YYYY-MM-DD.md`). **append-only 연대기**라
상호 링크 의무가 없고, 문서 고립 검사에서 면제된다(`check-doc-graph docs README.md --exempt progress`).

구조적 색인은 `.agents/context/project-index.yaml`(예: `phases`)이, 요약은 단계 summary 문서가 맡는다.
