# Commands 목록

> **SoT**: `.agents/commands/`
> 각 커맨드 파일의 human-readable 인덱스.

| 커맨드 | 파일 | 설명 |
|--------|------|------|
| _(등록된 커맨드 없음)_ | — | — |

---

> 새 커맨드 추가 시: `.agents/commands/` 에 파일 생성 → 이 표에 등록.
